<%= form_with(model: cashback) do |form| %>
  <% if cashback.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cashback.errors.count, "error") %> prohibited this cashback from being saved:</h2>

      <ul>
        <% cashback.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
